/* eslint-disable */
import { storiesOf } from '@storybook/react-native';
import React from 'react';
import Separation from '../../../src/components/Separation';

storiesOf('Separation', module).add('Default', () => (
    <Separation />
));
